import { createSlice } from "@reduxjs/toolkit";

const pdsFormSlice = createSlice({
  name: "pdsForm",
  initialState: {},
  reducers: {},
});

export const {} = pdsFormSlice.actions;
export default pdsFormSlice.reducer;
